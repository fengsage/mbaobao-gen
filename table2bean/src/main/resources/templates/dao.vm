/**
 * mbaobao.com Inc.
 * Copyright (c) 2012 All Rights Reserved.
 */
package ${env.daoPackage};

import java.util.List;
import java.lang.Integer;
import org.apache.ibatis.annotations.Param;
import ${env.entityPackage}.${java.className};

/**
 *
 * This tools just a simple useful tools, gen table to javabean
 *
 * "I hope this tools can save your time"
 *
 * Generated by <tt>Tabl2Bean</tt> on ${env.time}.
 *
 * @author fredzhu (kuci@mbaobao.com)
 * @version v1.0 ${env.time} $
 */
 
public interface ${java.className}Dao {

	public Integer insert(${java.className} $fieldName);
	
	public Integer get${java.className}Count(${java.className} param);
	
	public List<${java.className}> find${java.className}sByPage(@Param("param")${java.className} param,@Param("offset")Integer offset,@Param("rows")Integer rows);
	
}